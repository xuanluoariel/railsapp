<% 
  if @basic != nil
  	@session_number = @basic.session_number
	elsif params[:id] != nil
		@basic = Basic.find(params[:id])
		@session_number = @basic.session_number
	end
%>
<header class="navbar navbar-fixed-top navbar-fixed-top">
  <nav class="navbar navbar-inverse navbar-fixed-top">
	  <div class="container">
	    <div class="navbar-header">
	      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
	        <span class="sr-only">Toggle navigation</span>
	        <span class="icon-bar"></span>
	        <span class="icon-bar"></span>
	        <span class="icon-bar"></span>
	      </button>
	      <ul class="nav navbar-nav">
	      	<li class="active"><%= link_to "Occupant Behavior - Movement", root_path %></li>
	      	<% if @session_number != nil %>
	      		<li class="my_tab_label"><%= "Your session number is #{@session_number}."%></li>
	      	<% end %>
	      </ul>
	    </div>
	    <div id="navbar" class="collapse navbar-collapse">
	    	<ul class="nav navbar-nav pull-right">
        	<li class="active"><%= link_to "Home", root_path %></li>
      	</ul>
	    </div>
	  </div>
  </nav>
</header>